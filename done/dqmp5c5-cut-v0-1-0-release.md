# Cut v0.1.0 release

## Summary
Cut the first release: bump `__version__` to `0.1.0`, commit, tag `v0.1.0`, and create the GitHub Release — the stable channel that `trck update` consumes.

## Acceptance criteria
- [ ] `__version__ = "0.1.0"` in `trck`
- [ ] tag `v0.1.0` pushed
- [ ] GitHub Release `v0.1.0` created

## Notes
Release ritual: bump → commit → tag → GitHub Release.
