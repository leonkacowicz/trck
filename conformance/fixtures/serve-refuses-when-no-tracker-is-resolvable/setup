# Nothing is bound until the tracker is in hand. A listener started first would be a server
# answering every request with the same 500, on a port the user now has to go and free — the
# refusal a verb prints and exits with is the whole of what should happen here.
