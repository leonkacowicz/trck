# Same graph as deps-scoped-excludes-cousins; --full restores the weakly-connected
# component, so the cousin C reappears.
new "A" --id aaaaaaa --empty
new "B" --id bbbbbbb --requires aaaaaaa --empty
new "C" --id ccccccc --requires aaaaaaa --empty
new "D" --id ddddddd --requires bbbbbbb --empty
