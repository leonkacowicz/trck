# Same graph as deps-scoped-excludes-cousins; --full restores the weakly-connected
# component, so the cousin C reappears.
new "A" --id aaaaaaa
new "B" --id bbbbbbb --requires aaaaaaa
new "C" --id ccccccc --requires aaaaaaa
new "D" --id ddddddd --requires bbbbbbb
