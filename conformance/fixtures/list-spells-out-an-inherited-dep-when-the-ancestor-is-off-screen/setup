# The parent authored the dep; filtering to the child alone leaves the parent's row off
# screen, so the child's note spells the edge out and tags where it lives.
new "Dep" --id aaaaaaa --empty
new "Parent epic" --id bbbbbbb --requires aaaaaaa --empty
new "Child leaf" --id ccccccc --parent bbbbbbb --empty
