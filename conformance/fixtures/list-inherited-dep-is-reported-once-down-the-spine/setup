# Both mid and leaf inherit the same edge; only the highest row on screen states it.
new "Dep" --id aaaaaaa --empty
new "Top epic" --id bbbbbbb --requires aaaaaaa --empty
new "Child mid" --id ccccccc --parent bbbbbbb --empty
new "Child leaf" --id ddddddd --parent ccccccc --empty
