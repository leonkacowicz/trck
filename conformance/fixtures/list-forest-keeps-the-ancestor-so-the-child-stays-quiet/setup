# The nested forest keeps a match's ancestor spine as context, so the parent is on screen
# and the child does not repeat the inherited note.
new "Dep" --id aaaaaaa --empty
new "Parent epic" --id bbbbbbb --requires aaaaaaa --empty
new "Child leaf" --id ccccccc --parent bbbbbbb --empty
