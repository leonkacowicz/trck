# A parent's line reaches its children through inferred containment edges — before
# these, an epic printed "(no dependencies)".
new "Epic" --id aaaaaaa --empty
new "Kid one" --id bbbbbbb --parent aaaaaaa --empty
new "Kid two" --id ccccccc --parent aaaaaaa --empty
