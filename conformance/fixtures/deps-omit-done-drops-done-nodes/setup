# --omit-done removes the done middle node; the graph is recomputed, leaving two
# singleton components.
new "Base" --id aaaaaaa
new "Mid" --id bbbbbbb --requires aaaaaaa
new "Top" --id ccccccc --requires bbbbbbb
done bbbbbbb
