# Removing the last edge leaves an empty list in the echo and drops the default-valued key
# from the stored row.
new "Blocker" --id aaaaaaa --empty
new "Dependent" --id bbbbbbb --requires aaaaaaa --empty
