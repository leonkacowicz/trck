# Authored so id order (a,b,c) and dependency order (c before b before a) disagree;
# every requirement must still render above the issue that needs it. Created in
# dependency order because --requires needs its target to exist first.
new "Deepest" --id ccccccc --empty
new "Middle" --id bbbbbbb --requires ccccccc --empty
new "Head" --id aaaaaaa --requires bbbbbbb --empty
