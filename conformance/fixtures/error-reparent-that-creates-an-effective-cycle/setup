# Re-parenting can close a loop that neither the old nor the new edge closed alone: b
# depends on a, so putting a under b would make a wait on its own parent.
new "A" --id aaaaaaa
new "B" --id bbbbbbb --requires aaaaaaa
