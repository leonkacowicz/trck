[package]
name = "trck"
description = "Deterministic in-repo issue tracker"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

# No dependencies, and that is a design constraint carried over from the Python
# engine rather than an accident of being early: the binary is meant to be a single
# self-contained artifact that a repo can depend on for years.
[dependencies]

[lints]
workspace = true
