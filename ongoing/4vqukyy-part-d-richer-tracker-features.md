# Part D: richer tracker features

## Summary
Enhancements to the tracker data model beyond A+B, tracked as child issues: labels/tags, configurable custom fields, and a `reconfigure` verb.

## Acceptance criteria
- [ ] Child issues completed

## Notes
`index.jsonl` already preserves unknown keys, which eases additive fields.
